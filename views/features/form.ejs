<% locals.activePage = 'projects' %>
<%- include('../layout/header') %>

<div class="page-header">
  <h2><%= feature ? 'Edit Feature' : 'New Feature' %> — <%= project.title %></h2>
</div>

<% if (locals.error) { %><p class="error"><%= error %></p><% } %>

<form method="POST" action="<%= feature ? '/projects/'+project.id+'/features/'+feature.id : '/projects/'+project.id+'/features' %>">

  <label for="name">Feature Name</label>
  <input type="text" id="name" name="name"
         value="<%= feature ? feature.name : '' %>" required autofocus
         placeholder='e.g. "Build CI/CD for boarding pass"' />

  <label for="start_date">Start Date</label>
  <input type="date" id="start_date" name="start_date"
         value="<%= feature ? (feature.start_date||'') : '' %>" required />

  <label for="end_date">End Date</label>
  <input type="date" id="end_date" name="end_date"
         value="<%= feature ? (feature.end_date||'') : '' %>" required />

  <div style="margin-top:1.5rem;">
    <button type="submit">Save</button>
    <a href="/projects/<%= project.id %>">Cancel</a>
  </div>
</form>

<%- include('../layout/footer') %>
